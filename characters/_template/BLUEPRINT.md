# Template Character Blueprint

Purpose: minimal example character pack. Copy this folder to create a new character.

## Next Steps

- Add either `live2d/model.model3.json` or `avatar3d/model.vrm`.
- Edit `persona.md`.
- Configure `voice.yaml` when TTS is enabled.
- Update `character.yaml` state mappings to match available expressions/motions.
