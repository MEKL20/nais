# OpenSpec Blueprint

Purpose: spec-first planning artifacts for NAIS changes.

Use `changes/<change-name>/proposal.md`, `design.md`, `tasks.md`, and `specs/*/spec.md` before larger implementation work.
